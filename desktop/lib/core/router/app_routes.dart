/// Named routes used by the staff desktop app (admin + doctor). Patient
/// routes live in the mobile project.
abstract final class AppRoutes {
  static const splash = '/';
  static const login = '/login';
  static const doctor = '/doctor';
  static const admin = '/admin';
}
